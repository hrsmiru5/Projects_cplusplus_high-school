//Write a program that reads a natural number $n$ from the keyboard and constructs a matrix with $n$ rows and $n$ columns. The elements shall be assigned values based on their diagonals as follows:In the top-left corner (coordinates $(1, 1)$), the value is 1.In the adjacent positions (coordinates $(1, 2)$ and $(2, 1)$), the value is $n$.In the next diagonal positions (coordinates $(1, 3)$, $(2, 2)$, and $(3, 1)$), the value is 2.In the following positions (coordinates $(1, 4)$, $(2, 3)$, $ (3, 2)$, and $(4, 1)$), the value is $n-1$.And so on…In the second-to-last positions (coordinates $(n-1, n)$ and $(n, n-1)$), the value is $n$.In the final position (bottom-right corner, coordinates $(n, n)$), the value is 1.

#include <iostream>
using namespace std;
int main()
{
    int n,i,j,a[31][31];
    cin>>n;
    for(i=1;i<=n;i++)
    {
        for(j=1;j<=n-i+1;j++)
        {
            if((i+j)%2==0)
            {
             a[i][j]=(i+j)/2;
             a[n+1-j][n+1-i]=a[i][j];
            }
            else
                {
                    a[i][j]=n-(i+j)/2+1;
                    a[n+1-j][n+1-i]=a[i][j];
                }
        }
    }
    for(i=1;i<=n;i++)
    {
        for(j=1;j<=n;j++)
            cout<<a[i][j]<<' ';
        cout<<endl;
    }
    
    
    return 0;
}
