// Given an array with $n$ natural numbers, display the values from the array that are divisible by 10 in descending order.

#include <iostream>
using namespace std;
int main()
{
    int i,j,n,v[1001],aux,ok=0;
    cin>>n;
    for(i=0;i<n;i++)
        cin>>v[i];
    for(i=0;i<n-1;i++)
    {
        for(j=i;j<n;j++)
        {
            if(v[i]<v[j])
            {
                aux=v[i];
                v[i]=v[j];
                v[j]=aux;
            }
        }
    }
    for(i=0;i<n;i++)
    {
        if(v[i]%10==0)
        {
            cout<<v[i]<<' ';
            ok=1;
        }
    }
    if(ok==0)
        cout<<"NU EXISTA";
    return 0;
}
